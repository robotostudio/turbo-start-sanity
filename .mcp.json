{
  "mcpServers": {
    "turbo-start-sanity-kb": {
      "type": "http",
      "url": "${SANITY_CONTEXT_ENDPOINT}",
      "headers": {
        "Authorization": "Bearer ${SANITY_CONTEXT_TOKEN}"
      }
    }
  }
}
