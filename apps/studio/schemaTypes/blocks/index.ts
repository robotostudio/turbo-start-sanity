import { cta } from "@blocks/cta";
import { faqAccordion } from "@blocks/faq-accordion";
import { featureCardsIcon } from "@blocks/feature-cards-icon";
import { hero } from "@blocks/hero";
import { imageLinkCards } from "@blocks/image-link-cards";
import { subscribeNewsletter } from "@blocks/subscribe-newsletter";

export const pageBuilderBlocks = [
  hero,
  cta,
  featureCardsIcon,
  faqAccordion,
  imageLinkCards,
  subscribeNewsletter,
];
